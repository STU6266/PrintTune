export function SettingsPage() {
  return (
    <section className="page" aria-labelledby="settings-title">
      <h1 id="settings-title">Einstellungen</h1>
      <p>Lokale Einstellungen für PrintTune.</p>
    </section>
  );
}
